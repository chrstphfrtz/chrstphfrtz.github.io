<rss version="2.0">
  <channel>
    <title>chrstphfrtz</title>
    <description>Blog of chrstphfrtz.</description>
    <link>https://chrstphfrtz.github.io</link>
		
    <item>
      <title>Letterpress</title>
      <description><![CDATA[
			<p><em>This is a sample post to demonstrate how to write a post in Letterpress. You can see the rendered result <a href="http://wangling.me/2013/01/letterpress.html">here</a>.</em></p>

<hr />

<h3>Updated on 02/21/2013</h3>

<p>With the release of Letterpress 0.0.2 I can have fun with math easily. Simply attaching a “math” tag gains me the power of math: $E=m*c^2$.</p>

<hr />

<div class="code"><pre><span></span><code><span class="code-k">fn</span> <span class="code-nf">main</span><span class="code-p">()</span><span class="code-w"> </span><span class="code-p">{</span><span class="code-w"></span>
<span class="code-w">    </span><span class="code-kd">let</span><span class="code-w"> </span><span class="code-n">n</span><span class="code-w"> </span><span class="code-o">=</span><span class="code-w"> </span><span class="code-mi">5</span><span class="code-p">;</span><span class="code-w"></span>

<span class="code-w">    </span><span class="code-k">if</span><span class="code-w"> </span><span class="code-n">n</span><span class="code-w"> </span><span class="code-o">&lt;</span><span class="code-w"> </span><span class="code-mi">0</span><span class="code-w"> </span><span class="code-p">{</span><span class="code-w"></span>
<span class="code-w">        </span><span class="code-fm">print!</span><span class="code-p">(</span><span class="code-s">&quot;{} is negative&quot;</span><span class="code-p">,</span><span class="code-w"> </span><span class="code-n">n</span><span class="code-p">);</span><span class="code-w"></span>
<span class="code-w">    </span><span class="code-p">}</span><span class="code-w"> </span><span class="code-k">else</span><span class="code-w"> </span><span class="code-k">if</span><span class="code-w"> </span><span class="code-n">n</span><span class="code-w"> </span><span class="code-o">&gt;</span><span class="code-w"> </span><span class="code-mi">0</span><span class="code-w"> </span><span class="code-p">{</span><span class="code-w"></span>
<span class="code-w">        </span><span class="code-fm">print!</span><span class="code-p">(</span><span class="code-s">&quot;{} is positive&quot;</span><span class="code-p">,</span><span class="code-w"> </span><span class="code-n">n</span><span class="code-p">);</span><span class="code-w"></span>
<span class="code-w">    </span><span class="code-p">}</span><span class="code-w"> </span><span class="code-k">else</span><span class="code-w"> </span><span class="code-p">{</span><span class="code-w"></span>
<span class="code-w">        </span><span class="code-fm">print!</span><span class="code-p">(</span><span class="code-s">&quot;{} is zero&quot;</span><span class="code-p">,</span><span class="code-w"> </span><span class="code-n">n</span><span class="code-p">);</span><span class="code-w"></span>
<span class="code-w">    </span><span class="code-p">}</span><span class="code-w"></span>

<span class="code-w">    </span><span class="code-kd">let</span><span class="code-w"> </span><span class="code-n">big_n</span><span class="code-w"> </span><span class="code-o">=</span><span class="code-w"></span>
<span class="code-w">        </span><span class="code-k">if</span><span class="code-w"> </span><span class="code-n">n</span><span class="code-w"> </span><span class="code-o">&lt;</span><span class="code-w"> </span><span class="code-mi">10</span><span class="code-w"> </span><span class="code-o">&amp;&amp;</span><span class="code-w"> </span><span class="code-n">n</span><span class="code-w"> </span><span class="code-o">&gt;</span><span class="code-w"> </span><span class="code-o">-</span><span class="code-mi">10</span><span class="code-w"> </span><span class="code-p">{</span><span class="code-w"></span>
<span class="code-w">            </span><span class="code-fm">println!</span><span class="code-p">(</span><span class="code-s">&quot;, and is a small number, increase ten-fold&quot;</span><span class="code-p">);</span><span class="code-w"></span>

<span class="code-w">            </span><span class="code-c1">// This expression returns an `i32`.</span>
<span class="code-w">            </span><span class="code-mi">10</span><span class="code-w"> </span><span class="code-o">*</span><span class="code-w"> </span><span class="code-n">n</span><span class="code-w"></span>
<span class="code-w">        </span><span class="code-p">}</span><span class="code-w"> </span><span class="code-k">else</span><span class="code-w"> </span><span class="code-p">{</span><span class="code-w"></span>
<span class="code-w">            </span><span class="code-fm">println!</span><span class="code-p">(</span><span class="code-s">&quot;, and is a big number, halve the number&quot;</span><span class="code-p">);</span><span class="code-w"></span>

<span class="code-w">            </span><span class="code-c1">// This expression must return an `i32` as well.</span>
<span class="code-w">            </span><span class="code-n">n</span><span class="code-w"> </span><span class="code-o">/</span><span class="code-w"> </span><span class="code-mi">2</span><span class="code-w"></span>
<span class="code-w">            </span><span class="code-c1">// TODO ^ Try suppressing this expression with a semicolon.</span>
<span class="code-w">        </span><span class="code-p">};</span><span class="code-w"></span>
<span class="code-w">    </span><span class="code-c1">//   ^ Don&#39;t forget to put a semicolon here! All `let` bindings need it.</span>

<span class="code-w">    </span><span class="code-fm">println!</span><span class="code-p">(</span><span class="code-s">&quot;{} -&gt; {}&quot;</span><span class="code-p">,</span><span class="code-w"> </span><span class="code-n">n</span><span class="code-p">,</span><span class="code-w"> </span><span class="code-n">big_n</span><span class="code-p">);</span><span class="code-w"></span>
<span class="code-p">}</span><span class="code-w"></span>
</code></pre></div>

<p>I'm writing this post in <a href="http://www.iawriter.com">iA Writer</a> on my iPad while cozily lying in my recliner after lunch. I'm writing it in <a href="http://daringfireball.net/projects/markdown/syntax">Markdown</a>, not messy HTML. The first half was typed in on my MacBook Pro, also in iA Writer. I can publish it from my iPhone later by moving it to a special folder in my Dropbox when I'm out.</p>

<p>To make this whole writing and publishing experience possible, I built my own blogging system — <a href="https://github.com/an0/Letterpress">Letterpress</a>. </p>

<p>I've been using <a href="http://wordpress.org">WordPress</a> since July, 2009. It is almost good enough. I encountered several downtimes when MySQL hung. I don't know a thing about database so I had to reboot the whole machine. I even wrote 1 or 2 small WordPress plugins for my own use, which further consolidated my aversion to PHP. RSS/Atom feed generating is meaningless to me since I'm not famous and no one subscribe to my blog except myself. Comments are also just ego feeders and I guess I got more VIAGRA spams than sensible comments. Most importantly, the writing experience is not good, neither in its web editor nor in its mobile apps.</p>

<p>I just want a minimal blogging system with good writing experience. Only static text files, no dynamic HTML generated from database. No RSS/Atom. No comments. And no PHP.</p>

<p>That's how Letterpress was conceived and built. I believe you can guess how it is named.</p>

      ]]></description>
      <pubDate>Thu, 31 Jan 2013 00:00:00 -0000</pubDate>
      <guid isPermaLink="true">https://chrstphfrtz.github.io/2013/01/sample_post.html</guid>
      <link>https://chrstphfrtz.github.io/2013/01/sample_post.html</link>
    </item>
		
  </channel>
</rss>
